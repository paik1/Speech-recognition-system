% this program is for the genrating refmat(reference  matrix)
clc
clear all;
testmatrix=zeros(100,50);
for i=0:9
  for  j=0:9
      load(sprintf('%d_sample_%d.mat',i,j));
      testmatrix(10*i+j+1,:)=[newfilterbankarraydkktest zeros(1,50-length(newfilterbankarraydkktest))];
  end
end
   save('testmatrix','refmat');